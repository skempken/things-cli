# Things CLI Environment Variables

# Auth token for Things URL scheme (required for update commands)
# Get your token from:
# - Mac: Things → Settings → General → Enable Things URLs → Manage
# - iOS: Settings → General → Things URLs
THINGS_TOKEN=your-auth-token-here

# Usage:
# 1. Copy this file to .env (optional, or export directly)
# 2. Replace 'your-auth-token-here' with your actual token
# 3. Source it: source .env
# OR add to your shell profile (~/.zshrc, ~/.bashrc):
# export THINGS_TOKEN="your-actual-token"
